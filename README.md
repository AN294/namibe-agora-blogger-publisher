# NAMIBE AGORA Blogger Publisher

Publisher Express para Blogger API + Google OAuth 2.0.

Variáveis do Vercel: GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, APP_URL, GOOGLE_REDIRECT_URI.

Depois do primeiro deploy, use a URL real do Vercel e configure a mesma Redirect URI no Google Cloud OAuth Client.
